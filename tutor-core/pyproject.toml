[project]
name = "tutor-core"
version = "0.1.0"
description = "Domain, ports, gates and application services."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
